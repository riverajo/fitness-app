---
trigger: always_on
---

We use docker-compose for the full-stack development envirnmont. It should be running already check with `docker compose ps`. If it's not running prompt the user.

Both the frontend and backend are hot-reloaded in both containers.